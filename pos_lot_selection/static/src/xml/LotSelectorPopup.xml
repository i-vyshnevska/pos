<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2022 Camptocamp SA
     License LGPL-3.0 or later (https://www.gnu.org/licenses/lgpl). -->
<templates id="template" xml:space="preserve">

        <!-- <t t-inherit="pos_event_sale.EventRegistrationReceipt" t-inherit-mode="extension"> -->

        <t t-name="LotSelectorPopup" owl="1" >
            <div role="dialog" class="modal-dialog">
                <div class="popup popup-text">
                    <header class="title ">
                        <t t-esc="props.title"/>
                    </header>
                    <main class="body">
                        <t t-foreach="slected_items" t-as="item" t-key="item_index" id="lot_select">
                            <select class="popup-input">
                                <t t-debug="pdb"></t>
                                <option t-foreach="lots" t-as="lot" t-key="lot_index" selected="item.text == '5'" t-att-value="lot.name" >
                                    <t t-esc="lot.name"/> 
                                </option>
                            </select>
                        </t>
                        <!--  TODO dynamically add new lines if option selected -->
                        <select class="popup-input" id="new_select">
                            <option value="">Select Lot ID</option>
                            <option t-foreach="lots" t-as="lot" t-key="lot_index" t-att-value="lot.name" >
                                <t t-esc="lot.name"/> 
                            </option>
                        </select>
                    </main>
                    <footer class="footer">
                        <div class="button confirm" t-on-click="confirm">
                            Ok
                        </div>
                        <div class="button cancel" t-on-click="cancel">
                            Cancel
                        </div>
                    </footer>
                </div>
            </div>
        </t>

</templates>
